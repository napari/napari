name: Build Docs

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-napari-docs:
    name: Build docs on napari/napari-docs
    runs-on: ubuntu-latest
    steps:
      - name: Trigger workflow and wait
        uses: convictional/trigger-workflow-and-wait@v1.6.1
        with:
          owner: melissawm
          repo: napari-docs
          github_token: ${{ secrets.DEPLOY_DOCS }}
          github_user: melissawm
          workflow_file_name: deploy_docs.yml
          trigger_workflow: true
          wait_workflow: true
