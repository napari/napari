name: Remove "ready to merge" label

on:
  push:
    branches:
      - main
  schedule:
    - cron: "6 6 * * *" #every morning

permissions:
  pull-requests: write


jobs:
  remove_label:
    name: Remove ready to merge label
    uses: napari/shared-workflows/.github/workflows/remove_ready_to_merge.yml@main # v0.7.0
